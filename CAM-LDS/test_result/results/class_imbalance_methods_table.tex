\documentclass[10pt]{article}
\usepackage[margin=0.6in]{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage{ragged2e}
\pagestyle{empty}

\newcolumntype{L}[1]{>{\RaggedRight\hspace{0pt}}p{#1}}

\begin{document}

\begin{table}[!ht]
\centering
\begin{tabular}{L{3.2cm} L{2.6cm} L{9.5cm}}
\toprule
Category & Method & What it is \\
\midrule
Data Augmentation & EDA & Uses dictionary-based synonym replacements, random insertion, random swap, and random deletion \\
\cmidrule(lr){2-3}
 & TextCut & Randomly replaces small parts of the BERT representation of one instance with those of another \\
\cmidrule(lr){2-3}
 & ECRT & Learns to map encoder representations to a new space whose components are independent given the class, enabling generation of new minority examples by permuting/sampling components \\
\midrule
Loss Functions & FL (Focal Loss) & Down-weights instances the model is already confident on \\
\cmidrule(lr){2-3}
 & ADL & Extends Dice loss (which captures class-wise F1) with confidence-based down-weighting of easy predictions \\
\cmidrule(lr){2-3}
 & LDAM & Uses larger class-dependent margins for minority classes to improve their separation and generalization \\
\cmidrule(lr){2-3}
 & WBCE & Assigns higher weights to minority classes, but also increases the weight of their negative instances \\
\cmidrule(lr){2-3}
 & DB (Distribution-Balanced) & Adds ``Negative Tolerant Regularization'' --- caps the loss for negative classes once the model is already confident, instead of letting the punishment build up indefinitely \\
\midrule
Staged Learning & cRT & Two-stage re-training: normal training first, then class-balanced sampling in a second stage after freezing the representation weights \\
\cmidrule(lr){2-3}
 & ST (Sequential Targeting) & Models imbalanced classification as a continual-learning task with stages where the data gradually becomes more balanced \\
\midrule
Model Design & tau-norm & Normalizes classifier weights using a temperature hyperparameter tau \\
\cmidrule(lr){2-3}
 & SetConv & Learns class representatives from support sets using convolution kernels to capture intra/inter-class correlations \\
\cmidrule(lr){2-3}
 & ProtoBERT & Uses class centroids in a learned BERT feature space, classifies by similarity to class representatives \\
\cmidrule(lr){2-3}
 & HSCNN & Uses class representatives only for tail classes; head classes go through a standard text CNN \\
\bottomrule
\end{tabular}
\end{table}
\end{document}
